import './Spinner.css';

const Spinner = props => {
    return (
        <div className='spinner__wrapper'>
            <div className='spinner'></div>
        </div>

    );
}

export default Spinner;